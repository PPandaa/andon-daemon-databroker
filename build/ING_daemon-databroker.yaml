apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: andon-daemon-databroker
spec:
  rules:
  - host: databroker.ifactoryandondev.eks005.en.internal
    http:
      paths:
      - backend:
          serviceName: andon-daemon-databroker
          servicePort: 8080
        path: /