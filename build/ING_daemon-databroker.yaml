apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ifps-andon-daemon-databroker
spec:
  rules:
  - host: ifps-andon-daemon-databroker.tienkang.eks002.en.internal
    http:
      paths:
      - backend:
          serviceName: ifps-andon-daemon-databroker
          servicePort: 8080
        path: /