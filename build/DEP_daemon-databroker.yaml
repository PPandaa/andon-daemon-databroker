apiVersion: apps/v1
kind: Deployment
metadata:
  name: ifps-andon-daemon-databroker
spec:
  selector:
    matchLabels:
      run: ifps-andon-daemon-databroker
  replicas: 1
  template:
    metadata:
      labels:
        run: ifps-andon-daemon-databroker
    spec:
      containers:
      - name: ifps-andon-daemon-databroker
        image: iiicondor/andon-daemon-databroker:tienkang-eks002
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 10m
            memory: 64M
            ephemeral-storage: 64M
          limits:
            cpu: 40m
            memory: 128M
            ephemeral-storage: 128M
        ports:
        - containerPort: 8080
          name: http
