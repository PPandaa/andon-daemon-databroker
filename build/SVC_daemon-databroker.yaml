apiVersion: v1
kind: Service
metadata:
  name: andon-daemon-databroker
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: andon-daemon-databroker
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
